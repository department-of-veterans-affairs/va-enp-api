# async def test_one(sample_user, sample_service):
#     user = await sample_user()
#     await sample_service(created_by_id=user.id)


# async def test_two(sample_user, sample_service):
#     user = await sample_user()
#     await sample_service(created_by_id=user.id)
